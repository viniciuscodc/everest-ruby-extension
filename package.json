{
  "name": "everest-ruby",
  "publisher": "ViniciusOrtega",
  "displayName": "everest-ruby",
  "description": "Everest utils",
  "version": "1.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/viniciuscodc/everest-ruby-extension"
  },
  "engines": {
    "vscode": "^1.106.1"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "everest-ruby.newClass",
        "title": "Everest: New Ruby Class"
      },
      {
        "command": "everest-ruby.newContext",
        "title": "Everest: New Context"
      },
      {
        "command": "everest-ruby.newTest",
        "title": "Everest: New Test"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "everest-ruby.newClass",
          "when": "resourceScheme == 'file' || resourceScheme == 'vscode-remote'",
          "group": "navigation"
        },
        {
          "command": "everest-ruby.newContext",
          "when": "resourceScheme == 'file' || resourceScheme == 'vscode-remote'",
          "group": "navigation"
        },
        {
          "command": "everest-ruby.newTest",
          "group": "navigation",
          "when": "resourceExtname == .rb"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.46.3",
    "eslint": "^9.39.1",
    "esbuild": "^0.27.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
